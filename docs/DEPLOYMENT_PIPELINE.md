# Deployment pipeline

Deployment must be validated before changing the database. The canonical workflow keeps database migration deployment separate from the normal frontend/function deployment path.
